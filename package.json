{
  "name": "animal-merge-game",
  "version": "1.0.0",
  "private": true,
  "description": "A bilingual mobile-ready animal merge game.",
  "type": "module",
  "scripts": {
    "build": "node scripts/copy-web-assets.mjs",
    "cap:sync": "npm run build && npx cap sync android",
    "android:open": "npm run cap:sync && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/core": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3"
  }
}
